name: Build docker image

on:
  push:
  pull_request:
    branches:
      - '*'
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Build:parentDockerImage"
        run: docker build -t ghcr.io/smartmedsa/base-php:latest .
